---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: trafficwatches.traffic.example.com
spec:
  group: traffic.example.com
  names:
    kind: TrafficWatch
    listKind: TrafficWatchList
    plural: trafficwatches
    singular: trafficwatch
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TrafficWatch is the Schema for the trafficwatches API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TrafficWatchSpec defines the desired state of TrafficWatch
            properties:
              maxBandwidthPercent:
                description: MaxBandwidthPercent defines amount of traffic that will
                  cause a Node to be matrked as "unfit" as a floating point value
                type: string
            required:
            - maxBandwidthPercent
            type: object
          status:
            description: TrafficWatchStatus defines the observed state of TrafficWatch
            properties:
              nodes:
                description: Nodes current status of nodes
                items:
                  properties:
                    currentBandwidthPercent:
                      description: CurrentBandwidthPercent shows average use of bandwidth
                        in last 10s
                      type: string
                    currentTransmitTotal:
                      description: CurrentTransmitTotal
                      format: int64
                      type: integer
                    name:
                      description: Name of the Node
                      type: string
                    time:
                      description: Time
                      format: int64
                      type: integer
                    unfit:
                      description: Unfit is marked as "unfit"
                      type: boolean
                  required:
                  - currentBandwidthPercent
                  - currentTransmitTotal
                  - name
                  - time
                  - unfit
                  type: object
                type: array
            required:
            - nodes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
